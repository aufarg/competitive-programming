#include <bits/stdc++.h>

using namespace std;

int main()
{
    int ntc;
    scanf("%d", &ntc);
    for (int itc = 1; itc <= ntc; ++itc) {
        int r, c, k, m;
        scanf("%d%d%d%d", &r, &c, &k, &m);
        for (int i = 0; i < r; ++i) {
            string s;
            for (int j = 0; j < c; ++j) grid[i][j] = s[j];
        }
    }
    return 0;
}
