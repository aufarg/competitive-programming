#include <bits/stdc++.h>

using namespace std;

int main()
{
    int ntc;
    while (ntc--) {
        int n;
        scanf("%d", &n);
        for (int i = 0; i < n; ++i) scanf("%d", &a[i]);
        for (int len = 1; len < n; ++len) {
            for (int i = 0; i < n; ++i) {
            }
        }
    }
    return 0;
}
